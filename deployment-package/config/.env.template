# Agent Monitor Production Configuration

# Database Configuration
DB_PASSWORD=secure_production_password
DB_PORT=5432

# Monitor Configuration
MONITOR_PORT=8000
HOST_IP=localhost

# Agent Configuration
AGENT_NAME=Production Agent
AGENT_TYPE=LLM_AGENT
WORKLOAD_TYPE=api

# Security (Optional)
# Enable for secret file-based authentication
# USE_SECRETS=true
